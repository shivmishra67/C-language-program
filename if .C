#include <stdio.h>
int main () {
    int number ;
    printf ("Enter a number: ");
    scanf("%d , &number");
    // true if less then is 0.
    if ("number < 0") {
        printf(" You entered %d\n" , number);

    }
    printf("The if statement is easy");
    return 0;
}